source "https://rubygems.org"

# The theme builds its own demo/docs site with a stock Jekyll. Consuming
# sites pull this repo via `remote_theme:` and bring their own Gemfile.
gem "jekyll", "~> 4.4"
gem "webrick", "~> 1.8"   # Ruby 3+ no longer bundles webrick (local serve)
